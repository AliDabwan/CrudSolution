@model PersonForReturnDTO
<div class="container">
    <div class="row">
        <div class="col">
            <h2 class="text-Dark ">Add new person</h2>
        </div>
        <div class="col text-end mt-2">
            <a href="~/persons/index" class="btn btn-outline-primary"><i class="bi bi-arrow-left-square-fill"></i> Back to persons list</a>
        </div>
    </div>
    <form action="~/persons/create" method="post">
        <div class="form-group p-1 mx-3 mt-0">
            <label asp-for="Name"  class="form-label">Name:</label>
            <input type="text" class="form-control" asp-for="Name" placeholder="Enter name">
        </div>
        <div class="form-group p-1 mx-3">
            <label asp-for="Email" class="form-label">Email:</label>
            <inputasp-for ="Email" class="form-control" id="email" name="Email" placeholder="Enter email">
        </div>
        <div class="form-group p-1 mx-3">
            <label for="dob" class="form-label">Date of Birth:</label>
            <input type="date" class="form-control" id="dob" name="DateOfBirth">
        </div>
        <div class="form-group p-1 mx-3">
            <label class="form-label">Gender:</label>
            <div class="form-check ">
                <input class="form-check-input" type="radio" name="Gender" id="male" value="male">
                <label class="form-check-label" for="male">
                    Male
                </label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="Gender" id="female" value="female">
                <label class="form-check-label" for="female">
                    Female
                </label>
            </div>
        </div>
        <div class="form-group p-1 mx-3">
            <label for="country" class="form-label">Country:</label>
            <select class="form-select form-control" asp-for="CountryId" asp-items="ViewBag.Countries">
                <option value="">Please Select Country</option>
               @*  @foreach (CountryForReturnDto item in ViewBag.Countries)
                {
                    <option value="@item.Id">@item.Name</option>

                } *@
            </select>
            <div class="form-group p-1 mt-2">
                <label for="ReceiveEmails" class="form-check-label">Receive Emails:</label>
                <input type="checkbox" class="form-check-input ms-2" id="ReceiveEmails" name="ReceiveEmails" value="true">
            </div>
        </div>
        <button type="submit" class="btn btn-primary ms-3 mt-2">Create</button>
        @if (ViewBag.Errors != null)
        {
            <div class="text-danger mt-1">
                <ul>
   
                @foreach (string error in ViewBag.Errors)
                {
                    <li>
                        @error
                    </li>
                }
                </ul>
            </div>
        }
    </form>
</div>